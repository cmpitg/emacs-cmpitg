(setq hyde/git/remote "origin"          ; The name of the remote to which we should push
      hyde/git/remote "master"          ; The name of the branch on which your blog
                                        ; resides
      hyde/deploy-command  "git push"   ; Command to deploy
      )
